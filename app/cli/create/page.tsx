import { ReleaseCliGuide } from '../../../components/ReleaseCliGuide'

export const metadata = { title: 'co create | ConnectOnion', alternates: { canonical: '/cli/create' } }

export default function Page() {
  return <ReleaseCliGuide name="create" />
}
