import { ReleaseCliGuide } from '../../../components/ReleaseCliGuide'

export const metadata = {
  "title": "YouTube CLI for AI agents (co youtube) | ConnectOnion",
  "description": "Read your channel and uploads, and preview an upload before it happens, with your saved Google login — like co gmail.",
  "alternates": {
    "canonical": "/cli/youtube"
  }
}

export default function Page() {
  return <ReleaseCliGuide name="youtube" />
}
