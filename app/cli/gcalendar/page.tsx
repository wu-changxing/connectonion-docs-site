import { ReleaseCliGuide } from '../../../components/ReleaseCliGuide'

export const metadata = {
  "title": "Google Calendar CLI for AI agents, with Meet (co gcalendar) | ConnectOnion",
  "description": "List, read, create, update and delete Google Calendar events, find free slots and create Meet links — one command, using your saved Google login.",
  "alternates": {
    "canonical": "/cli/gcalendar"
  }
}

export default function Page() {
  return <ReleaseCliGuide name="gcalendar" />
}
