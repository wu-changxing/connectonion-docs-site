import { ReleaseCliGuide } from '../../../components/ReleaseCliGuide'

export const metadata = { title: 'co env | ConnectOnion', alternates: { canonical: '/cli/env' } }

export default function Page() {
  return <ReleaseCliGuide name="env" />
}
