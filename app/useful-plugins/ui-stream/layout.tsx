import { buildMetadata } from '../../../lib/seo'

export const metadata = buildMetadata('/useful-plugins/ui-stream')

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
